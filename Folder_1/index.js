    let a = +prompt('Enter the first number');
    let b = +prompt('Enter the second number');

    let c = (a > b) ? (a + b) :
    (a < b) ? (a * b) :
    (a == b) ? 'These the same numbers' : 'ww';


    console.log(a);
    console.log(b);
    console.log(c);

    // let b = (a >= 1) ? 'Positive':
    // (a < 0) ? 'Negative' :
    // (a === 0) ? 'Zero' : 'www';
    // console.log('New value is ' + b);
   



    
